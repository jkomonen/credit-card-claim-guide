# Credit Card Claim Guide (Minimal)

A tiny Vite + React demo showing two credit cards with hover animation, search filtering, and an inline option chooser.

Quick start:

```bash
npm install
npm run dev
```

Open the dev URL printed by Vite (usually http://localhost:5173).
